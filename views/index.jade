extends layout

block append content
    .tabbable.tabs-left
      ul.nav.nav-tabs
        each exerciseNumber in exerciseNumbers
            li: a(href="#tab#{exerciseNumber}", data-toggle="tab") Exercise #{exerciseNumber}
      .tab-content
        each exercise, id in exercises
            div.tab-pane(id="#{id}")
                !{exercise}
